
CREATE TABLE MONTHLY_ACCUM 
(
   MONTHLY_ACCUM_ID      ID_TYPE NOT NULL,
   PART_ITEM_ID          ID_TYPE NOT NULL,
   DOCUMENT_DATE         DATE_TYPE NOT NULL,
   IMPORT_AMOUNT         MONEY_TYPE,
   EXPORT_AMOUNT         MONEY_TYPE,
   BALANCE_AMOUNT1       MONEY_TYPE,
   BALANCE_AMOUNT2       MONEY_TYPE,
   LOCATION_ID           ID_TYPE,
   SIMULATE_FLAG         FLAG_TYPE,
   BATCH_ID              ID_TYPE,
   YYYYMM                CODE_TYPE,
   BIRTH_AMOUNT          MONEY_TYPE,

   CREATE_DATE           DATE_TYPE NOT NULL,
   CREATE_BY             ID_TYPE NOT NULL,
   MODIFY_DATE           DATE_TYPE NOT NULL,
   MODIFY_BY             ID_TYPE NOT NULL
);

ALTER TABLE MONTHLY_ACCUM ADD CONSTRAINT MONTHLY_ACCUM_ID_PK PRIMARY KEY (MONTHLY_ACCUM_ID);
ALTER TABLE MONTHLY_ACCUM ADD CONSTRAINT MONTHLY_ACCUM_LOCATION_ID_FK FOREIGN KEY (LOCATION_ID) REFERENCES LOCATION (LOCATION_ID);
ALTER TABLE MONTHLY_ACCUM ADD CONSTRAINT MONTHLY_ACCUM_PART_ID_FK FOREIGN KEY (PART_ITEM_ID) REFERENCES PART_ITEM (PART_ITEM_ID);

CREATE GENERATOR MONTHLY_ACCUM_SEQ;


ALTER TABLE BALANCE_ACCUM ADD YYYYMM CODE_TYPE;

