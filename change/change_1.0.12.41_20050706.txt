ALTER TABLE PAYMENT ADD INTERNAL_FLAG FLAG_TYPE;

ALTER TABLE PAYMENT ADD ACCEPT_BY ID_TYPE;
ALTER TABLE PAYMENT ADD CONSTRAINT PAYMENT_ACCEPT_BY_FK FOREIGN KEY (ACCEPT_BY) REFERENCES EMPLOYEE;

ALTER TABLE PAYMENT ADD TOTAL_AMOUNT MONEY_TYPE;

ALTER TABLE PAYMENT_ITEM ADD PAY_AMOUNT MONEY_TYPE;

ALTER TABLE CUSTOMER ADD CREDIT_AMOUNT MONEY_TYPE;


CREATE TABLE REGION
(
   REGION_ID          ID_TYPE NOT NULL,
   REGION_NO          CODE_TYPE NOT NULL,
   REGION_NAME        NAME_TYPE NOT NULL,

   CREATE_DATE        DATE_TYPE NOT NULL,
   CREATE_BY          ID_TYPE NOT NULL,
   MODIFY_DATE        DATE_TYPE NOT NULL,
   MODIFY_BY          ID_TYPE NOT NULL
);

ALTER TABLE REGION ADD CONSTRAINT REGION_ID_PK PRIMARY KEY (REGION_ID);

CREATE GENERATOR REGION_SEQ;


UPDATE PAYMENT SET INTERNAL_FLAG = 'Y';

COMMIT;
