
ALTER TABLE BALANCE_ACCUM ADD BALANCE_AMOUNT MONEY_TYPE;
ALTER TABLE BALANCE_ACCUM ADD TOTAL_INCLUDE_PRICE MONEY_TYPE;
ALTER TABLE BALANCE_ACCUM ADD LOCATION_ID ID_TYPE;

ALTER TABLE BALANCE_ACCUM ADD CONSTRAINT BALANCE_ACCUM_LOCATION_ID_FK FOREIGN KEY (LOCATION_ID) REFERENCES LOCATION;

ALTER TABLE EXPORT_ITEM ADD EXPORT_TOTAL_PRICE MONEY_TYPE;

/*
SELECT BA.DOCUMENT_DATE, PI.PART_NO, PI.PART_DESC, LC.LOCATION_NAME,
   BA.IMPORT_AMOUNT, BA.EXPORT_AMOUNT, BA.BALANCE_AMOUNT, BA.TOTAL_INCLUDE_PRICE
FROM BALANCE_ACCUM BA
LEFT OUTER JOIN PART_ITEM PI ON (BA.PART_ITEM_ID = PI.PART_ITEM_ID)
LEFT OUTER JOIN LOCATION LC ON (BA.LOCATION_ID = LC.LOCATION_ID)
WHERE (BA.PART_ITEM_ID = 7314)
AND (BA.BALANCE_AMOUNT <> 0)
AND (BA.LOCATION_ID = 254)
ORDER BY BA.DOCUMENT_DATE

SELECT * FROM EXPORT_ITEM EI
LEFT OUTER JOIN PART_ITEM PI ON (EI.PART_ITEM_ID = PI.PART_ITEM_ID)
LEFT OUTER JOIN LOCATION LC ON (EI.LOCATION_ID = LC.LOCATION_ID)
WHERE (EI.PART_ITEM_ID = 7314)
AND (EI.LOCATION_ID = 254)

**/