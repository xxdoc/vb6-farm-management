CREATE TABLE RECEIPT_CNDN
(
   RECEIPT_CNDN_ID       ID_TYPE NOT NULL,
   BILLING_DOC_ID        ID_TYPE NOT NULL,
   DO_ID                 ID_TYPE,
   CNDN_ID               ID_TYPE,
   CN_AMOUNT             MONEY_TYPE,
   DN_AMOUNT             MONEY_TYPE,

   CREATE_DATE           DATE_TYPE NOT NULL,
   CREATE_BY             ID_TYPE NOT NULL,
   MODIFY_DATE           DATE_TYPE NOT NULL,
   MODIFY_BY             ID_TYPE NOT NULL
);

CREATE GENERATOR RECEIPT_CNDN_SEQ;

ALTER TABLE RECEIPT_CNDN ADD CONSTRAINT RECEIPT_CNDN_ID_PK PRIMARY KEY (RECEIPT_CNDN_ID);
ALTER TABLE RECEIPT_CNDN ADD CONSTRAINT RECEIPT_CNDN_BLD_ID_FK FOREIGN KEY (BILLING_DOC_ID) REFERENCES BILLING_DOC;
ALTER TABLE RECEIPT_CNDN ADD CONSTRAINT RECEIPT_CNDN_DO_ID_FK FOREIGN KEY (DO_ID) REFERENCES BILLING_DOC;
ALTER TABLE RECEIPT_CNDN ADD CONSTRAINT RECEIPT_CNDN_CNDN_ID_FK FOREIGN KEY (CNDN_ID) REFERENCES BILLING_DOC;
